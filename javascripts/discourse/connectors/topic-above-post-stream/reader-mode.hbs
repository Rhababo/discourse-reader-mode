{{#if isReaderTopic}}
            <DButton
                @action={{fn (action "toggleReaderMode")}}
                @icon="book-reader"
                @translatedLabel="Reader Mode"
                class="btn-default filter-topic-owner-posts topic-owner-{{topicOwnerUsername}}"
            />
{{/if}}
