reader_category:
  type: string
  default: ""
  description:
    en: 'Enter the category title of the category you would like to apply Reader Mode to.'

reader_tag:
  type: string
  default: "reader-mode"
  description:
    en: 'Topics will have access to Reader Mode when given this tag.'

